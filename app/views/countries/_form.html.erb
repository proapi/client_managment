<%= form_for(@country) do |f| %>
    <% if @country.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@country.errors.count, "error") %> prohibited this country from being saved:</h2>

          <ul>
            <% @country.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="table">
      <table class="listing form" cellpadding="0" cellspacing="0">
        <tr>
          <th class="full" colspan="2">Formularz danych kraju</th>
        </tr>
        <tr>
          <td><strong><%= f.label :name, 'Nazwa kraju' %></strong></td>
          <td> <%= f.text_field :name %></td>
        </tr>
        <tr>
          <td><strong><%= f.label :short, 'Skrót identyfikacyjny kraju' %></strong></td>
          <td> <%= f.text_field :short %></td>
        </tr>
        <tr>
          <td><strong><%= f.label :currency, 'Waluta' %></strong></td>
          <td> <%= f.text_field :currency %></td>
        </tr>
        <tr>
          <td class="full" colspan="2"><%= f.submit "Zapisz" %></td>
        </tr>
      </table>
    </div>
<% end %>