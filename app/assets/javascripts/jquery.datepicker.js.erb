$(document).ready(function($) {
    $(".datepicker").datepicker().formatDate('dd-mm-yy', "2012-12-12");
    $('.ui-datepicker-current').live('click', function() {
           $(".datepicker").datepicker("setDate", date);
    });
});